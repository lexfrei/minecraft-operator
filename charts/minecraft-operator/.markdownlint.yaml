# Markdownlint configuration for helm-docs generated content
# helm-docs uses specific formatting that conflicts with some rules

# Disable line length - technical tables can be long
MD013: false

# Allow asterisk style lists (helm-docs generates these)
MD004: false

# Allow inline HTML (badges, etc.)
MD033: false

# Allow bare URLs
MD034: false

# Allow tables with inconsistent column widths
MD060: false

# Allow duplicate headings (Values section has many)
MD024: false
