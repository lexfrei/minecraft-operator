{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":semanticCommits",
    ":semanticCommitTypeAll(chore)"
  ],
  "labels": ["dependencies"],
  "regexManagers": [
    {
      "fileMatch": ["\\.github/workflows/.*\\.ya?ml$"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>.*?) depName=(?<depName>.*)\\n.*?[\"']?(?<currentValue>v?\\d+\\.\\d+\\.\\d+)[\"']?"
      ]
    },
    {
      "fileMatch": ["^Makefile$"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>.*?) depName=(?<depName>.*)\\n.*?[\"']?(?<currentValue>v?\\d+\\.\\d+\\.\\d+)[\"']?"
      ]
    },
    {
      "fileMatch": ["^build/Containerfile$"],
      "matchStrings": [
        "FROM (?<depName>.*?):(?<currentValue>[\\d.]+-[\\w.]+)",
        "# renovate: datasource=(?<datasource>.*?) depName=(?<depName>.*)\\n.*?=(?<currentValue>.*)"
      ],
      "datasourceTemplate": "docker"
    }
  ],
  "packageRules": [
    {
      "matchManagers": ["gomod"],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true
    },
    {
      "description": "Allow Renovate to bump go directive in go.mod",
      "matchManagers": ["gomod"],
      "matchDepNames": ["go"],
      "matchDepTypes": ["golang"],
      "rangeStrategy": "bump"
    },
    {
      "matchManagers": ["github-actions"],
      "automerge": true
    },
    {
      "description": "Allow patch and minor updates for Go base image used in Containerfile",
      "matchManagers": ["regex", "dockerfile"],
      "matchDatasources": ["docker"],
      "matchDepNames": ["docker.io/library/golang", "golang"],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true
    },
    {
      "matchDepPatterns": ["^ghcr.io/lexfrei/", "^docker.io/lexfrei/"],
      "enabled": false,
      "description": "Disable updates for our own images"
    }
  ],
  "vulnerabilityAlerts": {
    "enabled": true,
    "labels": ["security"]
  }
}
